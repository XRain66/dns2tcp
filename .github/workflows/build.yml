name: Build Multi-Platform Binaries

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch: [x86_64, aarch64]
    steps:
      - uses: actions/checkout@v4
      
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential autoconf automake gcc-multilib
          if [ "${{ matrix.arch }}" = "aarch64" ]; then
            sudo apt-get install -y gcc-aarch64-linux-gnu
          fi

      - name: Configure and Build
        run: |
          autoreconf -fi
          ./configure
          if [ "${{ matrix.arch }}" = "aarch64" ]; then
            make CC=aarch64-linux-gnu-gcc
          else
            make
          fi

      - name: Upload Linux Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dns2tcp-linux-${{ matrix.arch }}
          path: |
            client/dns2tcpc
            server/dns2tcpd

  build-windows:
    runs-on: windows-latest
    strategy:
      matrix:
        include:
          - arch: x64
            msystem: MINGW64
            mingw_prefix: mingw-w64-x86_64
          - arch: arm64
            msystem: CLANGARM64
            mingw_prefix: mingw-w64-clang-aarch64
    steps:
      - uses: actions/checkout@v4

      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: ${{ matrix.msystem }}
          update: true
          install: >-
            ${{ matrix.mingw_prefix }}-gcc
            ${{ matrix.mingw_prefix }}-autotools
            make
            
      - name: Configure and Build
        shell: msys2 {0}
        run: |
          autoreconf -fi
          ./configure
          make
          
      - name: Upload Windows Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dns2tcp-windows-${{ matrix.arch }}
          path: |
            client/dns2tcpc.exe
            server/dns2tcpd.exe 